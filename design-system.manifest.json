{
  "name": "@anclora/design-system",
  "version": "0.12.0",
  "_agentReadme": "This manifest is the machine-readable contract for coding agents working in any Anclora repo. Read componentStatus before adding a component (canonical = consume it, do not rebuild it; candidate = usable but not yet a hard requirement; needsConsumerEvidence = ships but has no confirmed real usage, verify before relying on it). Read profiles before choosing shell/density defaults for a new app. Read extensionPoints before overriding anything — if the property you need to change is not listed as a component's extension point, add a variant here instead of overriding the base selector. Read prohibitedPatterns before writing CSS that touches an .ac-* selector directly. Read theme before implementing light/dark in any new app — as of 0.7.0 the package ships a real canonical light theme, do not invent a local one. Read borders before deciding which border token to use — not every border needs 3:1, but a control/status border does, and there's a specific token for that. Full architecture: ANCLORA-DESIGN-SYSTEM-ARCHITECTURE.md. Full theme contract: docs/themes/light-dark-theme-contract.md. Full border/status/link contract: docs/tokens/semantic-borders-and-status.md. Full evidence: docs/ecosystem-audit/.",
  "borders": {
    "roles": {
      "subtle": { "token": "--border-subtle", "guarantee": "none — may be low contrast, decorative only", "use": "row dividers, section rules, anywhere the surface stays identifiable without the border" },
      "control": { "token": "--border-control", "guarantee": "WCAG 2.2 non-text >=3:1 against the surface it's painted on, verified against Core's own surfaces AND a real product surface fixture", "use": "form inputs/selects/textareas, and any future component where the border is the primary/only cue that something is a control or panel boundary", "since": "0.8.0" },
      "strong": { "token": "--border-strong", "guarantee": ">=3:1 by construction (derives from --line-strong)", "use": "emphasis, selected/focused structure, high-hierarchy separation" },
      "status": { "token": "--status-{success,warning,danger,review}-border", "publicToneMapping": "success→success, warning→warning, danger→danger, info→review", "guarantee": ">=3:1 against the badge's own surface, verified per tone per theme", "use": "ONLY .ac-status-badge — do not invent a local status border color; if a tone the DS doesn't cover is needed, that's Design System feedback, not a local addition", "since": "0.8.0" }
    },
    "rule": "WCAG 2.2 non-text contrast (1.4.11) applies when a border is necessary to perceive the component or its state — not to every visible line. A Modal's border, where elevation/shadow/backdrop already define the boundary, can stay on --border-default (unguaranteed). An input field's border, with no other strong boundary cue, needs --border-control. When genuinely unsure, prefer --border-control over silently relying on --border-default's unguaranteed contrast.",
    "note": "--border-default/--border are unchanged in 0.8.0 (still decorative-strength, not retroactively strengthened) — --border-control is additive specifically so ~30 existing consumers that never asked for a stronger default aren't visually changed. Full rationale, evidence, and the exact contrast numbers: docs/tokens/semantic-borders-and-status.md."
  },
  "linkText": {
    "rule": "A product accent valid for fills/icons/decoration is not automatically valid as normal-size text — 3:1 (non-text) and 4.5:1 (text) are different requirements, and an accent's contrast against its own canvas varies by product and by theme.",
    "darkFormula": "color-mix(in srgb, var(--accent) 80%, white 20%) — fixed in 0.8.0, was var(--accent) raw (Command Center's real accent measured 4.34:1 there, below the 4.5:1 AA text minimum)",
    "lightFormula": "color-mix(in srgb, var(--accent) 45%, black 55%) — unchanged since 0.7.0",
    "prohibited": "Using --accent (or any raw product accent) directly as --text-link, or as any normal-size text color, without verifying contrast against the actual canvas it will render on."
  },
  "theme": {
    "publicSelector": "data-theme",
    "supportedValues": ["dark", "light"],
    "defaultBehavior": "No data-theme attribute set -> :root values apply, which are identical to [data-theme=\"dark\"] (backward compatible with every pre-0.7.0 consumer).",
    "tokenEntrypoint": "tokens/semantic.css",
    "dsOwns": ["visual theme semantics per role (--bg, --text-primary, --status-danger-text, etc.)", "WCAG 2.2 AA contrast for every semantic text/surface pair it defines"],
    "appOwns": ["system preference detection (prefers-color-scheme)", "persistence of user choice", "the theme-switch UI control", "SSR-safe pre-hydration script that sets data-theme before first paint"],
    "mustNotRedefine": ["the meaning of a semantic role (e.g. what counts as 'danger')", "a third theme state", "--focus-ring or any accessibility-load-bearing token, for aesthetic reasons"],
    "brandVsTheme": "--accent (and other .product-anclora-* brand tokens in themes/product.css) is theme-invariant — the same brand color is used in both dark and light. Only its DERIVED usages (--text-link, --focus-ring, --text-on-accent) are recomputed per theme, because a brand accent that passes contrast in dark mode can fail it in light mode (verified: Core's own silver accent is 2.04:1 as raw text on the light canvas, fails AA; darkened via color-mix it's 7.62:1).",
    "answersForAgents": {
      "I need light/dark theme for a new Anclora app. Should I create my own palette?": "No. Set data-theme=\"dark\"|\"light\" on <html> and consume the canonical semantic tokens from tokens/semantic.css — do not copy a token block from anclora-talent or anclora-command-center, and do not invent your own selector/class/attribute for theme.",
      "Can I set a custom brand accent while still using the canonical themes?": "Yes — that's what themes/product.css's .product-anclora-* layer is for. Override --accent (and, only if your brand identity genuinely requires it, --bg/--fg*) there; do not touch the semantic role definitions in tokens/semantic.css itself."
    }
  },
  "forms": {
    "contractDoc": "./docs/components/forms-and-inputs.md",
    "architecture": "Field owns label/hint/message relationships; Control owns value/focus/disabled/readonly/invalid state.",
    "canonicalControls": ["field-input", "field-search", "field-select", "field-textarea", "ac-choice-control", "ac-choice-control--switch"],
    "requiredSemantics": "Use required or aria-required on the actual control; visible asterisk is supplemental only.",
    "errorSemantics": "Use aria-invalid=true and aria-describedby for the visible error message; never rely on color alone.",
    "disabledVsReadonly": "disabled is unavailable and not submitted; readonly text remains focusable, readable and submitted.",
    "combobox": { "status": "evidence-required", "rule": "Do not promote a shared combobox until two materially different consumers validate the API and keyboard model." },
    "extensionPoints": ["layout wrapper", "field density only where a sanctioned variant exists", "product copy and option data"],
    "prohibitedPatterns": ["placeholder-only labels", "color-only errors", "clickable div controls without native semantics", "consumer-local focus rings or control borders", "premature shared combobox abstraction"]
  },
  "dataTable": {
    "contractDoc": "./docs/components/data-table.md",
    "cssEntrypoint": "./src/components/data-table.css",
    "layers": {
      "primitives": "native table semantics: caption, thead, tbody, tr, th and td",
      "pattern": ".ac-data-table with .ac-data-table__scroll",
      "behaviours": "compose sorting, selection, filters, pagination and bulk actions; application owns state"
    },
    "defaultResponsiveStrategy": "horizontal overflow for simple tables; documented mobile list/card transformation for action-heavy tables",
    "density": ["default", "compact"],
    "sorting": "native button inside th plus aria-sort; no sorting engine",
    "selection": "application-owned state using canonical Forms checkbox; data-selected is presentation only",
    "status": "compose ac-status-badge semantic tones",
    "rowActions": "compose ac-button, including ac-button--icon only with an accessible name",
    "pagination": "separate application composition; no table-owned pagination component",
    "sticky": "opt-in via ac-data-table--sticky; sticky columns are not supported",
    "dataGrid": "evidence-required",
    "extensionPoints": ["compact density", "data alignment", "sticky header opt-in", "product-owned column content"],
    "prohibitedPatterns": ["MegaDataTable", "data-fetching table wrapper", "generic sorting engine", "local table-action-button", "inline spreadsheet editing", "virtualisation", "column resize", "pinned columns", "drag reorder"],
    "answersForAgents": {
      "I need to display records": "Use the canonical native table contract and .ac-data-table; do not create a local table family.",
      "I need row actions": "Compose the canonical Button/link/menu semantics; do not create .table-action-button or .row-menu-button.",
      "I need filters": "Compose canonical Forms outside the table; the table does not own filter controls.",
      "I need an editable spreadsheet-like grid": "Do not force Data Table; classify as DATA_GRID_EVIDENCE_REQUIRED.",
      "I need mobile": "Use horizontal overflow for simple tables; use a documented accessible list/card composition for action-heavy tables."
    }
  },
  "feedback": {
    "contractDoc": "./docs/components/feedback-and-system-states.md",
    "architecture": "Separate persistent alerts, transient toasts, full-surface empty/error states, short-lived loading, determinate progress, operation processing and structural skeleton placeholders. No generic mega-feedback component.",
    "components": ["alert", "toast", "emptyState", "errorState", "loading", "progress", "processing", "skeleton"],
    "statePrecedence": ["loading", "error", "empty", "content"],
    "runtimeOwnership": "The Design System owns anatomy, tokens and semantic guidance. Each product owns data fetching, queues, timers, portals, retry logic and live-region timing.",
    "ariaGuidance": "Use role=status for polite progress/loading updates and role=alert only for urgent dynamic errors. A static informational alert may have no live role. Progress exposes min/max/value when determinate; indeterminate progress must not fake a value.",
    "answersForAgents": {
      "an API request failed for the page": "Use Error State with a recovery action.",
      "a field is invalid": "Use the Forms field message with aria-invalid and aria-describedby.",
      "an operation succeeded": "Use Toast or an in-context confirmation; do not use a full-page error/success state.",
      "an import or export is running": "Use Loading for unknown duration, Progress for a trustworthy numeric value, and Processing when the operation has meaningful activity context.",
      "there are no records": "Use Empty State; distinguish first-use, no-results, filtered and permission variants through content and product-owned context."
    },
    "evidenceRequired": ["notificationCentre", "jobDashboard", "resumableJobManager"]
  },
  "overlays": {
    "contractDoc": "./docs/components/overlays-menus-and-popovers.md",
    "architecture": "CSS visual contract plus semantic HTML and an optional consumer-owned accessible runtime. The Design System does not ship a universal Overlay component or mandate Radix.",
    "decisionRules": {
      "three contextual actions": "Menu, usually opened by an explicit Button/icon-only Button trigger.",
      "short supplemental help": "Tooltip on hover and keyboard focus; it is non-interactive and never the only accessible name.",
      "rich contextual controls": "Non-modal Popover; the page remains interactive and the panel owns only its contextual content.",
      "mobile side panel": "Drawer/Sheet when content is substantial or a desktop contextual surface needs a mobile composition.",
      "blocking confirmation": "Modal/Dialog, not Popover or Menu.",
      "searchable options": "Combobox contract; currently evidence-required and not a generic Popover."
    },
    "canonical": ["menu", "popover", "tooltip", "drawer"],
    "runtimeOwnership": {
      "menu": "Consumer-owned or recommended headless primitive; runtime owns roving focus, keyboard navigation, dismissal and focus return.",
      "popover": "Consumer-owned or recommended headless primitive; runtime owns positioning, collision handling, focus strategy and dismissal.",
      "tooltip": "Consumer-owned or recommended tooltip primitive; runtime owns hover/focus timing and relationship; content remains non-interactive.",
      "drawer": "Consumer-owned or recommended dialog/sheet primitive; modal drawers require focus containment and inert/backdrop behaviour."
    },
    "evidenceRequired": ["contextMenu", "nestedMenu", "commandPalette", "megaMenu", "nestedDrawer", "dockingPanel"],
    "prohibitedPatterns": ["MegaOverlay", "generic div menu items without keyboard semantics", "tooltip used as an icon accessible name", "interactive controls inside Tooltip", "manual focus trap duplicated per consumer", "arbitrary z-index scale", "generic Popover used for Combobox or Select semantics", "Popover used for blocking confirmation", "Modal used for a lightweight command list"]
  },
  "componentStatus": {
    "canonical": [
      "button", "surfacePanel", "formField", "dataTable", "statusBadge", "emptyState", "modal",
      "themeSwitcher", "languageSwitcher", "stepper", "chapterRail", "templateCatalog",
      "previewOverlay", "previewControls", "workspaceStage", "workflowShell", "editorShell",
      "alert", "toast", "errorState", "loading", "progress", "processing", "skeleton",
      "menu", "popover", "tooltip", "drawer",
      "textEditor", "exportSuite"
    ],
    "needsConsumerEvidence": [
      "appTile", "metricCard", "sectionHeading", "sidebarNav", "tabs", "breadcrumb", "subnav",
      "statStrip", "editorStudio", "topbar"
    ],
    "_note": "canonical = at least one real consumer verified in code (see ANCLORA-DESIGN-SYSTEM-COMPONENT-INVENTORY.md). needsConsumerEvidence = shipped as CSS, no confirmed real usage found as of 2026-09-21 — usable as a candidate, do not describe it to a user as battle-tested."
  },
  "profiles": {
    "P-MKT": {
      "name": "Marketing / Landing",
      "description": "Public, unauthenticated, single-scroll narrative page. No persistent nav shell.",
      "mayOverride": ["spacing density (looser)", "hero/CTA emphasis", "headline typography family"],
      "mustNotOverride": ["semantic color meaning", "base radii", "focus/disabled behavior", "light/dark theme contract"]
    },
    "P-WKS": {
      "name": "Workspace / Operational",
      "description": "Authenticated, persistent sidebar/topbar shell, high information density: tables, entity lists, forms, modals, status badges.",
      "mayOverride": ["component density/size defaults (use .ac-button--compact, not a new class)", "navigation shell defaults", "contextual emphasis of status"],
      "mustNotOverride": ["semantic color meaning", "base radii", "focus/disabled behavior", "component base anatomy (see prohibitedPatterns)", "light/dark theme contract"]
    },
    "_note": "Full app-to-profile assignment: ANCLORA-DESIGN-SYSTEM-PRODUCT-MATRIX.md. An app with no strong shell/density need needs no profile ('Core / default' is valid) — do not force one."
  },
  "extensionPoints": {
    "button": {
      "sanctioned": [
        "--ac-button-bg", "--ac-button-fg", "--ac-button-border", "--ac-button-shadow",
        "--ac-button-padding-x", "--ac-button-min-height", "--ac-button-radius"
      ],
      "sanctionedVariants": ["--primary", "--secondary", "--ghost", "--destructive", "--sm", "--lg", "--compact", "--icon"],
      "note": "--compact (0.5.0) is the canonical 'smaller/denser button for a workspace surface'. --icon (0.6.0) is the canonical square icon-only shape — composes with EVERY variant and EVERY density, all 16 combinations are implemented and technically supported (e.g. `ac-button ac-button--ghost ac-button--icon` or `ac-button ac-button--secondary ac-button--compact ac-button--icon`); it is NOT a separate component, do not build '.icon-button'/'.table-action-button'/'.dashboard-icon-button' locally. Design guidance (not a blocked combination): avoid `--destructive --compact --icon` for a high-consequence action — a small unlabeled icon button is an easy mis-tap target for something irreversible; prefer default (44px) density for destructive icon actions, or add a confirmation step. An icon-only button MUST have an aria-label (or aria-labelledby) — the icon itself is never an accessible name, and a title/tooltip does not substitute for one. Do not use --icon for a toggle control that needs aria-pressed, or for a control that needs a specialized primitive (segmented toggle, etc.) — see prohibitedPatterns.",
      "answersForAgents": {
        "I need a square icon-only button": "Use `ac-button ac-button--<variant> ac-button--icon` (add `ac-button--compact` for the dense/36px version). Do not create a local class.",
        "Can I use --destructive --compact --icon together?": "Yes, technically supported — but for a high-consequence/irreversible action prefer the default (44px, non-compact) density instead, since a small unlabeled icon target is an easy mis-tap for something destructive. Not a blocked combination, just guidance.",
        "Is .ac-button--icon safe against a local .ac-button anatomy override in this consumer app?": "Yes as of 0.6.0 — --icon uses a compound selector (.ac-button.ac-button--icon) specifically hardened against the cascade bug found in the anclora-talent Wave 1 pilot. --compact and the color variants are NOT hardened the same way yet; if this consumer has a local `.ac-button { ... }` override, scope it with :not(.ac-button--compact) etc. — see docs/ecosystem-audit/05-button-contract-hardening.md."
      }
    }
  },
  "prohibitedPatterns": [
    "Inventing a local status border color (e.g. a consumer-specific --status-danger-border) instead of using the canonical --status-*-border tier — added 0.8.0 specifically so consumers stop needing to do this.",
    "Using --accent (or any raw product accent) as normal-size text (including --text-link) without verifying contrast — a fill/icon-safe accent is not automatically text-safe. See linkText.",
    "Using --border-subtle or --border-default where recognizing the component genuinely depends on the border being visible (e.g. a form input) — use --border-control instead.",
    "Globally strengthening --border-subtle or --border-default to fix one component's contrast problem instead of using --border-control on that component — this changes every consumer that never asked for a stronger default.",
    "Overriding border-radius, font-size, font-weight, or text-transform directly on the bare .ac-button selector in a consumer app instead of using a --compact/--sm/--lg/--icon variant or an --ac-button-* custom property.",
    "Building a local icon-only button class ('.icon-button', '.table-action-button', '.dashboard-icon-button', or similar) instead of composing `ac-button ac-button--<variant> ac-button--icon`.",
    "Shipping an .ac-button--icon (or any icon-only control) without an aria-label/aria-labelledby, or relying on a tooltip/title as the accessible name.",
    "Repurposing .ac-button as a toggle/pressed-state control (needs aria-pressed) or a menu/segmented-control primitive it wasn't designed for.",
    "Creating a new parallel button/card/modal class family in a consumer app (e.g. '.app-button', '.dashboard-button') when a canonical .ac-* component with a suitable variant already exists or could exist as one — propose a variant here instead.",
    "Redefining semantic tokens (--status-*, --danger, --success, --warning, --focus-ring) with app-local hex values instead of consuming the semantic layer.",
    "Inventing a new theme-toggle mechanism (a fourth alternative to the data-theme attribute contract) in a new app.",
    "Treating a shadcn/ui-generated component's default HSL/OKLCH palette as final without wiring it to Anclora's semantic/component tokens.",
    "Building a data-fetching MegaDataTable or generic sorting/query engine in the Design System.",
    "Replacing semantic tables with generic div grids without a documented responsive/accessibility reason.",
    "Making every table row clickable or relying on tr onClick without an equivalent keyboard interaction.",
    "Creating table-specific checkbox, select, input, status or pagination variants when canonical primitives compose.",
    "Building a generic MegaFeedback component that merges alert, toast, empty, error, loading and progress semantics.",
    "Using role=alert for every informational message or exposing a fake progress value for indeterminate work.",
    "Replacing an actionable operation indicator with a spinner-only or skeleton-only surface.",
    "Creating a consumer-local status palette, toast queue or notification centre without evidence and a documented contract.",
    "Creating a universal MegaOverlay that merges Menu, Popover, Tooltip, Drawer and Modal semantics.",
    "Using Tooltip for interactive content or as the only accessible name of an icon-only control.",
    "Using Popover for Combobox/Select semantics or for a blocking confirmation.",
    "Adding consumer-local arbitrary z-index values or duplicating focus traps without a documented runtime need."
  ],
  "deprecated": [
    { "api": "--coin-syncxml", "replacement": "--coin-guesthub", "since": "0.4.1", "reason": "product rename, alias kept for one release" },
    { "api": ".product-anclora-syncxml", "replacement": ".product-anclora-guesthub", "since": "0.4.1", "reason": "product rename, alias kept for one release" }
  ],
  "knownGaps": [
    { "token": "--accent-mint", "usedBy": ["editorStudio.css", "editorShell.css", "textEditor.css"], "issue": "referenced but never defined anywhere in the package — discovered during the 0.7.0 theme audit, not introduced by it. Do not invent a value for this without a product decision on what 'mint' should represent; see docs/themes/light-dark-theme-contract.md §8." },
    { "token": "--shadow-*", "issue": "theme-invariant (same black-based value in dark and light) as of 0.7.0 — not redefined per data-theme. Acceptable simplification for now, documented as a scoped-out follow-up, not an oversight." }
  ],
  "entrypoints": {
    "system": "./src/system.css",
    "legacy": "./colors_and_type.css",
    "tokens": {
      "core": "./src/tokens/core.css",
      "semantic": "./src/tokens/semantic.css"
    },
    "taxonomy": {
      "tier": "./src/taxonomy/tier.css",
      "domain": "./src/taxonomy/domain.css",
      "archetype": "./src/taxonomy/archetype.css",
      "role": "./src/taxonomy/role.css",
      "clusters": "./src/taxonomy/clusters.css"
    },
    "themes": {
      "product": "./src/themes/product.css"
    },
    "foundations": {
      "typography": "./src/foundations/typography.css",
      "primitives": "./src/foundations/primitives.css"
    },
    "components": {
      "index": "./src/components/index.css",
      "button": "./src/components/button.css",
      "surfacePanel": "./src/components/surface-panel.css",
      "appTile": "./src/components/app-tile.css",
      "metricCard": "./src/components/metric-card.css",
      "sectionHeading": "./src/components/section-heading.css",
      "languageSwitcher": "./src/components/language-switcher.css",
      "themeSwitcher": "./src/components/theme-switcher.css",
      "formField": "./src/components/form-field.css",
      "sidebarNav": "./src/components/sidebar-nav.css",
      "chapterRail": "./src/components/chapter-rail.css",
      "templateCatalog": "./src/components/template-catalog.css",
      "stepper": "./src/components/stepper.css",
      "tabs": "./src/components/tabs.css",
      "breadcrumb": "./src/components/breadcrumb.css",
      "subnav": "./src/components/subnav.css",
      "statStrip": "./src/components/stat-strip.css",
      "dataTable": "./src/components/data-table.css",
      "statusBadge": "./src/components/status-badge.css",
      "emptyState": "./src/components/empty-state.css",
      "toast": "./src/components/toast.css",
      "alert": "./src/components/alert.css",
      "errorState": "./src/components/error-state.css",
      "loading": "./src/components/loading.css",
      "progress": "./src/components/progress.css",
      "processing": "./src/components/processing.css",
      "skeleton": "./src/components/skeleton.css",
      "menu": "./src/components/menu.css",
      "popover": "./src/components/popover.css",
      "tooltip": "./src/components/tooltip.css",
      "modal": "./src/components/modal.css",
      "drawer": "./src/components/drawer.css",
      "previewOverlay": "./src/components/preview-overlay.css",
      "previewControls": "./src/components/preview-controls.css",
      "workspaceStage": "./src/components/workspace-stage.css",
      "workflowShell": "./src/components/workflow-shell.css",
      "editorStudio": "./src/components/editor-studio.css",
      "editorShell": "./src/components/editor-shell.css",
      "textEditor": "./src/components/text-editor.css",
      "exportSuite": "./src/components/export-suite.css",
      "topbar": "./src/components/topbar.css"
    }
  },
  "recommendedImport": "@anclora/design-system/system.css"
}
